# Guidelines Token Discipline Update

Updated `guidelines.md` to make token conservation a first-class rule. The new guidance tells Codex to prefer short prompts over huge single-pass efforts, avoid echoing raw tool output, cap verification loops after one implementation pass and one focused check, and pause before repeated browser or status churn. I also pointed the record instruction at the active `Dev/AgentRef/Records` folder so future updates use the path that actually exists.
