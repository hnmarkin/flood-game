Simplified evacuation routing to a one-route prototype. `EvacuationController` now chooses one baseline-risk source zone, routes to the nearest active placed shelter from `ShelterManager`, caches a `SimpleEvacuationRoute`, and sends path cells to the visualizer without scoring, selection, city exits, mitigation, danger, or missing-road state. `EvacuationRouteVisualizer` now clears old visuals and draws one visible LineRenderer through tile centers only. `ActionsPanelController` now routes `action_Button5` through `ToggleEvacuationPreview()`.
