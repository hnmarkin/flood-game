# Issue 26 agent workflow

Added the required GitHub triage labels; documented safe `gh` authentication plus label bootstrap and verification; and established unambiguous single-context routing with an intentionally empty glossary and ADR index. Verified authenticated `gh` list/create/label/view with temporary issue #28, then closed it. No credentials, domain glossary entries, or ADR decisions were added.
