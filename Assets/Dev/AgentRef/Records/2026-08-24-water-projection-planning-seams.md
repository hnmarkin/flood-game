# Water Projection Planning Seams

- Recorded that scenario profile changes replace the water baseline and re-resolve active modifier contributions without compounding values.
- Added a Dev-only `Dev_ProjectionController` with an intentionally empty, warning-emitting `CalculateHazards` seam for later hazard-overlay work.
